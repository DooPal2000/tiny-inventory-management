<% layout('layouts/boilerplate')%>

    <main class="px-3">
        <div class="text-center">
            <img src="img/vegetable.png" style="width:300px; height: 300px;" alt="로고"
                class="img-thumbnail mx-auto d-block mb-3">
            <p class="lead"> Tiny Inventory Management <br> 소규모 재고관리 서비스 <br><br> 방문을 환영합니다. <br><br>
                <% if(!currentUser) { %>
                    <a href="/login" class="btn btn-lg btn-secondary font-weight-bold border-white bg-black">
                        로그인
                    </a>
                    <% } else if(currentUser.role=='user' && currentUser.isActive ){ %>
                        <a href="/product" class="btn btn-info btn-lg">
                            재고 페이지
                        </a>
                        <a class="nav-link" href="/product"></a>
                    <% } else { %>
                        <a href="/admin" class="btn btn-danger btn-lg">
                            관리자 페이지
                        </a>
                    <% } %>


        </div>
    </main>